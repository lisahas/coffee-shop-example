html
  head
    title Coffee shops users
    style(type="text/css").
      table, th, td {
          border: 1px solid black;      
       }
       label {
          display:block;
          font-size:0.5em;      
       }
      
  body
    h1#myHeading Users
    p.firstParagraph 
    table.table-style
        each user in users 
            tr 
                td=user.id 
                td
                    a(href='./user-ratings/' + user.id) #{user.username}